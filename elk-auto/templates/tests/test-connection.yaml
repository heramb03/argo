apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "elk-setup.fullname" . }}-test-connection"
  labels:
    {{- include "elk-setup.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "elk-setup.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
